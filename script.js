function startGame(game) {
  if (game === "Memory") {
    // Redirect to memory game page
    window.location.href = "memory.html";
  } else if (game === "Reaction") {
    // Redirect to reaction game page
    window.location.href = "reaction.html";
  } 
}
